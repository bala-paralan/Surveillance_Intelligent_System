# TASK-038 — Use-case Visualization Layer

**Date:** 2026-04-24  
**Agent:** Frontend

## Summary
Implemented the glyph-based fusion outcome visualization layer on the live AOI map.

## Files created
- `src/frontend/src/api/fusion.ts` — `listOutcomes`, `getFusionWeights`, `updateFusionWeights` API calls with full `FusionOutcome` types
- `src/frontend/src/store/glyphStore.ts` — Zustand store managing glyph lifecycle: `addGlyph`, `purgeStale` (decay-based TTL), `clearAoi`
- `src/frontend/src/components/map/GlyphLayer.tsx` — React component that polls `/aoi/:id/outcomes` every 5 s, places MapLibre Marker instances per outcome, and runs a 1 s purge interval

## Files modified
- `src/frontend/src/components/map/AoiMap.tsx` — Added `mapInstance` state exposed after map `load` event; renders `<GlyphLayer>` when `activeAoiId` is set
- `src/frontend/src/index.css` — Added `@keyframes glyph-pulse` (scale 1→1.3→1, opacity 1→0.5→1, 1.5 s infinite) and `.glyph-pulse` class; also added `@keyframes critical-pulse` for CRITICAL alert border animation

## Glyph types rendered
`footstep_radius` (pulsing circle), `tunnel` (triangle SVG), `vehicle` (rectangle SVG), `gunshot` (explosion circle SVG), `drone` (hexagon SVG), `animal` (◆), `human`/`group` (👤 with count badge), `unknown` (?)

## Confidence colour scheme
- Green: confidence ≥ 0.8  
- Yellow: confidence 0.5–0.8  
- Red: confidence < 0.5
