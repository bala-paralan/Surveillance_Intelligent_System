# TASK-009 — Docker Compose Dev Stack

**Date:** 2026-04-24
**Agent:** DevOps

## What was built

Full Docker Compose dev environment for SurveillanceOS with all services wired together.

### Files created

| File | Purpose |
|---|---|
| `docker-compose.yml` | Orchestrates all 7 services on `sis-net` bridge network |
| `infra/docker/Dockerfile.backend` | Multi-stage Node 22 Alpine image; copies compiled `dist/` |
| `infra/docker/Dockerfile.analytics` | Python 3.11-slim; installs requirements, runs uvicorn |
| `infra/docker/Dockerfile.frontend` | Node 22 builder → nginx:alpine SPA server on port 5173 |
| `infra/docker/nginx.conf` | SPA fallback + `/api/` proxy to backend:3001 + HLS proxy |
| `.env.example` | Template for all required environment variables |

### Services

| Service | Image / Build | Port | Healthcheck |
|---|---|---|---|
| postgres | `postgis/postgis:16-3.4` | 5432 | `pg_isready` |
| redis | `redis:7-alpine` | 6379 | `redis-cli ping` |
| backend | `Dockerfile.backend` | 3001, 9091 | `GET /health` |
| analytics | `Dockerfile.analytics` | 8001, 9092→9091 | `GET /health` |
| frontend | `Dockerfile.frontend` | 5173 | — |
| prometheus | `prom/prometheus:v2.51.0` | 9090 | — |
| grafana | `grafana/grafana:10.4.0` | 3000 | — |

### Notes
- Depends-on conditions use `service_healthy` for postgres + redis
- Raw video never stored on application server; ffmpeg output path handled by stream-service
- Prometheus + Grafana added here (TASK-011 deliverable co-located)
