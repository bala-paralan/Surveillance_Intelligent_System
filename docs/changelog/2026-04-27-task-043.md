# TASK-043 — Multi-Sensor Border UI Requirements

**Date:** 2026-04-27
**Agent:** Frontend (lead)

## Summary
Translated the operator-first multi-sensor border console UI requirements into a runnable build. Lands the canonical detection/threat data contract, geo-anchored headline generator, severity rules engine, enriched threat detail drawer, alert-feed row enrichments, per-sensor map idioms, layer/basemap controls, multi-pane drag-drop + global critical banner, and i18n scaffolding.

## Files created

### Shared types (canonical detection / threat contract)
- `src/shared/types/sensor.ts` — `SensorType`, `Classification` taxonomy + critical-family helpers
- `src/shared/types/detection.ts` — `Detection` per §7.6 + `GeoPoint`
- `src/shared/types/threat.ts` — `Threat`, `ThreatContribution`, `Severity`
- `src/shared/types/index.ts` — barrel

### Frontend libs (pure)
- `src/frontend/src/lib/headline.ts` — `buildHeadline`, formatters, `fusedScoreExplanation`
- `src/frontend/src/lib/severity.ts` — `computeSeverity` rules engine + per-AOI policy override
- `src/frontend/src/lib/recommendedAction.ts` — default playbook strings (§14 Q6 placeholder)
- `src/frontend/src/lib/__tests__/headline.test.ts` — 20 tests
- `src/frontend/src/lib/__tests__/severity.test.ts` — 14 tests

### Threat drawer (§5.3)
- `src/frontend/src/components/threats/ThreatDetailDrawer.tsx` — full §5.3 drawer with mini-map, timeline, contributing sensors, fused-score explanation, recommended action, RBAC-gated engineering link
- `src/frontend/src/components/threats/ThreatMiniMap.tsx` — schematic SVG with anchor + radius + bearing arrow (only when `direction_deg` present, per Q3) + classification icon
- `src/frontend/src/components/threats/IllustrativeIcon.tsx` — per-classification + per-sensor SVG icon set
- `src/frontend/src/components/threats/ContributingSensorsList.tsx`
- `src/frontend/src/components/threats/DetectionTimeline.tsx`

### Map visuals (§7.1–7.5)
- `src/frontend/src/components/map/SensorGlyph.tsx` — distinct visual idiom per sensor type: seismic ring (+ arrow only when bearing present), acoustic bearing cone, thermal footprint+blob, LIDAR fence + footprint, PTZ FOV cone; continuous-event pulse animation
- `src/frontend/src/components/map/MapControls.tsx` — layer toggles (AOI / assets / detections / fused threats) + basemap switch (Default / OSM / Satellite gated until tile licensing is configured per §13)

### Multi-pane polish (§10)
- `src/frontend/src/components/layout/AoiLibraryDrawer.tsx` — toolbar popover with draggable AOI list
- `src/frontend/src/components/alerts/GlobalCriticalBanner.tsx` — sticky CRITICAL banner across all panes with synthesised audio cue + persistent mute toggle

### Threat ↔ Alert adapter
- `src/frontend/src/api/threats.ts` — `fusionOutcomeToThreat`, `alertToThreat`, plus glyph→sensor + outcome-class→classification mappings; re-exports canonical `Threat` types

### i18n scaffolding (§12)
- `src/frontend/src/i18n/index.ts` — `t()` accessor, `setLocale`
- `src/frontend/src/i18n/locales/en.ts` — English dictionary: classification, sensor, severity, headline phrases, formatting

## Files modified
- `src/frontend/tsconfig.json` — added `@shared/*` path alias and `../shared` include
- `src/frontend/vite.config.ts` — added `@shared` resolve alias
- `src/frontend/src/api/alerts.ts` — added `escalateAlert`, `markAlertFalse` shims for §9 hooks
- `src/frontend/src/components/alerts/AlertDetailModal.tsx` — now a thin wrapper that adapts AlertPublic → Threat and renders `ThreatDetailDrawer`
- `src/frontend/src/components/alerts/AlertTicker.tsx` — adds AOI badge, sensor icon, absolute timestamp on hover; passes context to drawer
- `src/frontend/src/components/alerts/AlertFeed.tsx` — full §5.2 row (severity chip + sensor icon + headline + AOI tag + timestamp + Ack/Escalate/Mark False/Open) with severity filter and sort toggle
- `src/frontend/src/components/map/AoiMap.tsx` — wires `MapControls` (layer visibility + basemap switch including inline OSM raster style)
- `src/frontend/src/components/map/AoiPane.tsx` — drop target for AOI library drag
- `src/frontend/src/components/map/GlyphLayer.tsx` — renders fusion outcomes via `SensorGlyph` (per-sensor idiom + continuous-event pulse)
- `src/frontend/src/pages/OperatorDashboard.tsx` — mounts `GlobalCriticalBanner` + `AoiLibraryDrawer`
- `src/frontend/src/components/threats/IllustrativeIcon.tsx` — sensor labels resolve through i18n

## Verification
- 34/34 unit tests pass (`npx vitest run src/lib/__tests__`)
- `tsc --noEmit` clean for all touched files; only pre-existing `import.meta.env` typing error in `client.ts` remains (separate ticket)
- Operator default route still flows to `/console` with no waveform/RTSP/port surfaces (§11 holds)

## Open questions surfaced
Q1 (live registry vs static), Q4 (severity threshold sign-off), Q5 (sound-cue policy per AOI), Q6 (recommended-action authorship) are deferred to stakeholder review. Q2 (operator can draw AOI) and Q3 (direction only on advanced sensors) were resolved during this session — Q3 implemented as: seismic glyph + mini-map arrow render only when `direction_deg` is supplied; acoustic falls back to 360° reach when bearing absent.

## Out of scope
- True geo-anchored MapLibre layers for cones/fences/FOVs (current implementation uses HTML markers placed at map centre — same placeholder behaviour as the prior GlyphLayer; needs sensor-id → coordinates lookup wired before promotion)
- Time/AOI/sensor-type filters in the alert feed (severity filter + sort toggle present; remaining filters need backend list endpoints)
- Backend implementations of `/alerts/:id/escalate` and `/alerts/:id/false-positive` — frontend hooks present per §9
- Locales other than English
- Mobile surfaces (§13)
