# TASK-042 — Engineering View

**Date:** 2026-04-24  
**Agent:** Frontend

## Summary
Replaced the placeholder EngineeringPage with a fully functional tabbed engineering dashboard covering sensor management, waveform inspection, fusion threshold editing, and GIS sync logs.

## Files created
- `src/frontend/src/api/engineering.ts` — `listSyncLogs`, `updateFusionWeights`, `patchSensorHealth` API calls
- `src/frontend/src/components/engineering/SensorTable.tsx` — Sensor table with health dot, type, truncated ID, BOP zone, last heartbeat; ADMIN-only "Maintenance" toggle button (calls `PATCH /sensors/:id/health` with `health=OFFLINE`)
- `src/frontend/src/components/engineering/WaveformPlaceholder.tsx` — Animated HTML5 Canvas sine wave UI placeholder; sensor + time-range dropdowns; disclaimer message for engineering SDK requirement
- `src/frontend/src/components/engineering/ThresholdsEditor.tsx` — Textarea JSON editor for fusion weights (from `GET /fusion/weights`); Save button calls `PUT /fusion/weights` (ADMIN only); inline JSON validation + success/error toast
- `src/frontend/src/components/engineering/SyncLogTable.tsx` — GIS sync log table with status colour coding (PENDING/RUNNING/SUCCESS/FAILED), timestamps, added/updated/removed counts, error message column

## Files modified
- `src/frontend/src/pages/EngineeringPage.tsx` — Replaced placeholder with tabbed layout (Sensors / Waveforms / Thresholds / Sync Logs); wrapped in `RequireAuth` + `EngineeringRoute` guards

## Security
- All write actions (Maintenance toggle, Save weights) are ADMIN-only, enforced client-side via `hasRole('ADMIN')`
- No RTSP credentials or PII in any component
